version: '2'
services:
  react.jpotv.yangarch.net:
    container_name: react.jpotv.yangarch.net
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /root/.ssh:/root/.ssh
    ports:
      - "21080:3000"
